import React from "react";

const Button = () => {
  return (
    <button className="py-2 font-semibold tracking-wide text-white bg-blue-500 rounded-lg shadow-lg hover:bg-blue-600">
      Sign In
    </button>
  );
};

export default Button;
